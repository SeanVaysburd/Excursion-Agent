# Semantic retrieval layer for the excursion-planning agent.
# Everything runs locally. No API keys.
#
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#   python demo.py
#
# First run downloads the all-MiniLM-L6-v2 weights (~90 MB) from the
# HuggingFace hub; after that it is fully offline.

llama-index-core>=0.12,<0.15
llama-index-embeddings-huggingface>=0.5,<0.7
llama-index-vector-stores-chroma>=0.4,<0.7
chromadb>=0.6,<2
